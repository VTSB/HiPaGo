<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>HiPaGo</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!--
	  Task D (iOS best-effort background download).

	  UIBackgroundModes / `processing` lets iOS run our registered
	  BGProcessingTask ("com.hipago.app.download") while the app is backgrounded,
	  so pending gallery downloads can make best-effort progress via the Rust
	  bypass core (see DownloadBackgroundTask.swift / DownloadWorkerPlugin.swift).
	  `fetch` is added so a short BGAppRefreshTask-style opportunistic window can
	  also kick the same scheduler if the OS prefers to grant that instead.

	  BGTaskSchedulerPermittedIdentifiers MUST list every identifier we submit to
	  BGTaskScheduler — iOS rejects (and crashes on register of) any identifier
	  not declared here. It must match the constant used in AppDelegate.register
	  and DownloadBackgroundTask.taskIdentifier exactly.

	  DEVICE-PENDING: the Xcode project's "Background Modes" *capability*
	  (Signing & Capabilities → Background Modes → Background processing /
	  Background fetch) must ALSO be enabled in the .xcodeproj — that toggle is a
	  build-setting/entitlement that cannot be set from this Info.plist alone and
	  cannot be configured in the sandbox. Verify it on-device during QA.
	-->
	<key>UIBackgroundModes</key>
	<array>
		<string>processing</string>
		<string>fetch</string>
	</array>
	<key>BGTaskSchedulerPermittedIdentifiers</key>
	<array>
		<string>com.hipago.app.download</string>
	</array>
</dict>
</plist>
